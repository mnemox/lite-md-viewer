# SQLite database (created at runtime)
*.db
*.db-shm
*.db-wal

# Graph export bundles (created at runtime)
attachments/

# Vector index files (rebuildable from the _vectors table)
vectors/
*.hnsw

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vs/
*.user
