# NOTE: install from the repository root so the editable path below resolves:
#   .venv\Scripts\python -m pip install -r src\litemd_viewer\requirements.txt
# (pip resolves relative paths in a requirements file against the current directory).
-e ./src/local-vector

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
SQLAlchemy>=2.0.30
watchdog>=4.0.0
python-multipart>=0.0.9

# Streaming HTTP client to the local Ollama server (local analysis chat).
httpx>=0.27.0

# Local CPU embeddings via ONNX Runtime (no PyTorch).
fastembed>=0.3.6
